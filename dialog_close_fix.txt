✅ DIALOG SCHLIESSEN-FUNKTION REPARIERT!

**PROBLEM:**
- X-Button oben rechts im OnboardingWizard funktionierte nicht
- Dialog konnte nicht über das Schließen-Icon geschlossen werden
- Benutzer hatte keine Möglichkeit, den Dialog zu schließen

**LÖSUNG IMPLEMENTIERT:**
```javascript
<Dialog open={isOpen} onOpenChange={(open) => {
  if (!open) {
    // When dialog is closed, act like "Später entscheiden"
    onComplete();
  }
}}>
```

**WAS PASSIERT JETZT:**
1. **X-Button funktioniert** - Dialog kann geschlossen werden
2. **Gleiche Funktionalität wie "Später entscheiden"** - Benutzer kommt ins Dashboard ohne Vereinszuordnung
3. **Konsistente Benutzerführung** - Egal ob X-Button oder "Später entscheiden", gleiches Verhalten

**VORHER:**
- onOpenChange={() => {}} - Dialog konnte nicht geschlossen werden
- X-Button war funktionslos

**NACHHER:**
- onOpenChange prüft, ob Dialog geschlossen wird
- Bei Schließung wird onComplete() aufgerufen
- Benutzer kann frei wählen: X-Button oder "Später entscheiden"

**USER-FLOW:**
```
Willkommen-Dialog
├── X-Button klicken → onComplete() → Dashboard ohne Verein
├── "Verein suchen" → Vereinssuche
├── "Später entscheiden" → onComplete() → Dashboard ohne Verein
└── ESC-Taste → onComplete() → Dashboard ohne Verein
```

**STATUS: 🎯 REPARIERT!**

Das Dialog-Schließen funktioniert jetzt perfekt und verhält sich wie "Später entscheiden"!